<div class="container">
<h1> Users</h1>

<table>
	<tr>
		<th>Name</th>
	</tr>

	<% @users.each do |user| %>
		<tr>
			
			<td><%= link_to user.name, user_path(user.name), :method => "get" %></td>
		</tr>
	<% end %>
</table>
</div>