<div class="container">
	<h1>Register Team</h1>

	<%= form_for :team, url: teams_path do |f| %>
		<p>
			<%= f.label :name %>
			<%= f.text_field :name %><br>

			<%= f.label :user_id %>
			<%= f.text_field :user_id %><br>
			


		</p>

		<p>
			<%= f.submit %>
		</p>
	<% end %>
</div>
